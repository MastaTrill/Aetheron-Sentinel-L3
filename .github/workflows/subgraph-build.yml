name: Subgraph build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  merge_group:

permissions:
  contents: read

jobs:
  subgraph-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: PR mode note
        if: github.event_name == 'pull_request'
        run: echo "Subgraph build is deferred to push/merge-group pipeline in this repository mode."
      - name: Build subgraph (push/merge_group)
        if: github.event_name == 'push' || github.event_name == 'merge_group'
        run: |
          npm ci --legacy-peer-deps
          npm run codegen
          npm run build
