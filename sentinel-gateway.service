[Unit]
Description=Sentinel FastAPI Gateway
After=network.target

[Service]
User=ubuntu
WorkingDirectory=/app
ExecStart=/usr/local/bin/uvicorn sentinel_gateway_prototype:app --host 0.0.0.0 --port 8000 --workers 4
Restart=always

[Install]
WantedBy=multi-user.target
