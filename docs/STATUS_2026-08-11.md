# Gate Status Snapshot — 2026-08-11

**Operator note:** Generated from committed release-evidence. Does not authorize any Base Mainnet broadcast.

## Controlled redeployment path (#210 / #215 / #235)

| Gate | Status | Evidence |
|------|--------|----------|
| Exact deployment manifest | **complete** | `release-evidence/sentinel-mainnet/redeployment/deployment-manifest.json` |
| Protected Base Sepolia rehearsal | **complete** | `base-sepolia-rehearsal.json` |
| 57% Aetheron treasury from inception | **verified on Sepolia** | beneficiary `0xA4737aa4b1E8a3C8f221BE9E55F5BDa307eCC1Fa`, share `570000000000000000` |
| Independent RPC reproduction | pending | after mainnet deploy |
| Authority + beneficiary verification | pending | after mainnet deploy |
| Explicit mainnet authorization | **pending** | `mainnet-authorization.json` (reset after PR #262) |
| Base Mainnet deployment | pending | — |
| Authorized buy/sell smoke test | pending | — |
| Immutable evidence package | pending | final validator |

### Confirmed Sepolia rehearsal facts

- Chain: Base Sepolia (`84532`)
- Token: `0x3555976fecf045833D6E148C42035170bA1337Ab`
- Tx: `0x77117f06adb7c9355659244929e6143785d5b66a75b40e2732912205fed7c433`
- Receipt status: success
- `safety.baseMainnetAuthorized`: false
- `safety.mainnetTransactionProduced`: false

### Mainnet authorization (blocking)

`mainnet-authorization.json` is intentionally `pending-authorization` because PR #262 changed deployment-bearing dependency metadata. A **fresh** time-bounded wallet signature must bind the final merged security-patched commit before any protected mainnet workflow may run.

Required fields still null:

- `authorization.authorizedAtUtc`
- `authorization.authorizedCommit`
- `authorization.signature`
- `limitations.expiresAt`
- `riskAcceptance.acceptedBy` / `acceptedAtUtc`

## Guardrails (#169)

Separate from token redeployment. Still requires verified-paused Base Sepolia deployment of the frozen three-contract core (SentinelInterceptor, CircuitBreaker, RateLimiter) and its own evidence packet.

## Explicit non-claims

- Legacy Base Mainnet SENTINEL token/pool remains **non-canonical**.
- No Base Mainnet deployment, liquidity, trading, or public-funds acceptance is authorized by this snapshot.
- Closing #210 requires the full gate table above to reach complete + final validator exit 0.

## Next operator actions

1. Confirm local `npm run security:base-sepolia-rehearsal` (or closure validator `--mode=readiness`) still passes against current main.
2. Fill and sign a new `mainnet-authorization.json` against the post-#262 commit (or wait for any further security patches).
3. Only then run the protected mainnet deployment workflow.
4. Complete dual-RPC, authority verification, smoke test, SHA256SUMS, and `--mode=final`.
